:orphan:

``graphem`` package
===================

The public Python objects are documented once in :doc:`api_reference`. Keeping
a single generated API index prevents duplicate Sphinx targets while preserving
this legacy page for inbound links from older releases.
